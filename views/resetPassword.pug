doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title Reset Password
        link(rel="icon" type="image/x-icon" href="/assets/palomitas.png")
        link(rel="stylesheet" type="text/css" href="/styles.css")
    body
        header(id="formatHeader")
            button(type="button" onclick="history.back()") &#8678       
            h1.main-title RESET PASSWORD
        p Please, enter a new password and repeat below:
        form(action=`/resetpassword/${token}` method="post" class="loginForm")
            p(id="regexExplanation") Passwords must contain upper and lower letters, a number, a symbol and be at least 8 characters long
            input(type="password" name="newPassword" placeholder="New password")
            input(type="password" name="newPasswordCheck" placeholder="Repeat new password")
            input(type="submit" value="Change password")

    script(src="/scripts.js")